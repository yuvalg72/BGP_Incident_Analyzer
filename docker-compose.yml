services:
  bgp-analyzer:
    build: .
    container_name: bgp-incident-analyzer
    ports:
      - "${BGP_ANALYZER_PORT:-17991}:17991"
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    read_only: true
    tmpfs:
      - /tmp:size=256m
