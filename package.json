{
  "name": "bgp-incident-analyzer-assets",
  "private": true,
  "version": "0.1.0",
  "description": "Deterministic repository asset rendering and JavaScript validation.",
  "scripts": {
    "render:diagrams": "node scripts/render-diagrams.js",
    "check:js": "node --check app/static/app.js && node --check scripts/render-diagrams.js && node --check scripts/compare-diagrams.js"
  },
  "devDependencies": {
    "sharp": "0.35.4"
  }
}
